#include "security/crypt.h"

/*#include <sodium.h>*/
#include <dpp/discordvoiceclient.h>

int mln::crypt::init()
{
    /*if (!dpp::discord_voice_client::sodium_initialised) {
        if (sodium_init() < 0) {
            throw dpp::exception("sodium_init() failed");
        }
        dpp::discord_voice_client::sodium_initialised = true;
    }*/
    return -1;
}
